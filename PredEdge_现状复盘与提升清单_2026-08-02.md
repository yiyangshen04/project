# PredEdge 现状复盘与提升清单

**2026-08-02** · 6 方向侦察 + 6 组对抗核验（13 agent / 47 条发现 / 20 CONFIRMED · 25 PARTIAL · 2 REFUTED）
数据窗口：生产日志 8.8 天全量（4,225 tick）、trade-ledger 全历史、notify-state 近 4 周、Gamma/CLOB/data-api 实测

> **与 08-01 复盘的关系**：昨天已做过一轮 10-agent / 52 发现的全面复盘（见记忆 `prededge-review-2026-08-01`）。本轮是**独立的第二次盲测**——事前未读那份结论，六个方向各自重新取证。
>
> **收敛的部分（交叉验证，可信度提高）**：总账量级（可修复错失 $30–40）、所有 skip 决策复核正确或零成本、`fill100` 是 paper 池 P0、**补仓窗口是最大单笔遗漏**、限价帽未按边缩放、缺 per-event 聚合帽、RPC fatal tick、日志周截断销毁现场、tick 行缺时间戳、`persistScanResult` 只走 Web UI 路由。两轮独立取证得出同一批结论，说明这些不是单次采样的偶然。
>
> **本轮新增（08-01 未覆盖）**：费率表全量实测（九档 + `rebateRate` 新字段 + 23% 免费）、data-api 分页 4000→10000、**Polygon 出块 2s→1.50s 导致 lookback 常量短 25%**、`totalLeft` 毛敞口 bug、07-31 洪水规模与 `llmPending` 静默淘汰 404–413 条、判读缓存被冲光、scan-notify 通知闸门的定量画像（21/28 信号走无方向后门、7/21 双腿通知、11 封重复邮件、28 token 回测 +0.4%/笔）、pUSD 迁移对监控的误判风险、具体哪两个 RPC 已死及唯一可用替换。
>
> **需要并轨的口径差异**：补仓窗口两轮分别记 +$35（3 个钱包 $80 在 0.68–0.70）与 +$26（85 股 ≤0.69），同一现象不同记账口径，量级一致；RPC fatal 频率 0.4%/4 天 vs 0.19%/8.8 天。

---

## TL;DR

**系统是健康的，钱没有被闸门挡住，但我们也几乎赚不到钱——瓶颈是标的物本身没有盘口。**

8.8 天全量：1,512 个链上澄清事件 → 9 个 🟢 双确认腿 → 其中 6 腿市场已关闭、2 腿书是空的 → **1 笔可成交**。那 1 笔赚了 +$22.96。

三个反直觉的核实结果：

1. **"错过的机会"基本不存在。** 逐笔查完，被 skip 的 8 次里 6 次是结算后补发澄清（市场已关，判对）、2 次是真空盘（实测 07-27 之后到结算零成交，机会成本 ≈$0）。**我在初步快照里说那两腿"方向全对被白丢了"，是错的**——方向确实对，但当时没有任何对手方，taker 和人工都吃不到。
2. **唯一一笔真金的错过，是成交之后。** 07-28 成交后 4.5 分钟内，同一个盘陆续新挂出约 85 股 ≤0.69 的卖单——**全部落在我们自己的限价里**——被别人吃走了，全部结算 $1。乐观口径 +$26 毛利。系统没去拿，因为 tokenId 去重是二值封锁：这个 token 有敞口了就永久 skip，没有加仓路径。
3. **诚实年化要下修。** 从 bt3 的乐观 $5–12k 下修到 **+$130~510/年**（$50/笔 × +21.3%/笔 × 1–4 笔/月），与既有"诚实年化 +$150~350"一致。

最该动的三件事按顺序：**成交后复访补仓** → **日志轮转 + 机会落库**（决定 8 月中 go/no-go 有没有数据）→ **换掉两个已死的 RPC**。

最不该动的：放松任何执行闸门（收益上限实测 $0）、抢速度（我们已领先第二名 34 秒）、调高单笔上限（盘口深度只有 ~$52）。

---

## 一、错过的机会 —— 逐笔核实

| # | 时间 | 事项 | 核实结论 | 机会成本 |
|---|---|---|---|---|
| 1 | 07-28 | 飓风 7+ / 1-3 两腿 `bestAskAtSignal=null` skip | **真空盘**。data-api 全量成交：两盘最后一笔分别停在 07-27，信号时点后到结算**零成交**（17 笔 / 5 笔全生命周期）。三腿最终全部 resolved NO，方向确实对，但无对手方 | **≈$0**（上限 $3） |
| 2 | 07-14 | Iran Gulf State 被"预告模板家族"一刀切 skip | 链上澄清落地 **9 秒内**市场已到 0.99，我们根本来不及 | **≈$0**（既没省也没亏） |
| 3 | — | 6 条「市场已关闭，无可执行盘口」 | **6/6 判对，0 误判**。全部是"结算后补发澄清"——官方主流行为（6/9） | **$0** |
| 4 | 近 4 周 | scan-notify 28 个去重 token 各下 $50 | 已结算 14 笔 **+$2.60（+0.4%/笔）**；未结算 14 笔按现价 MTM −$105.55。**期望 ≈ 0 到负** | 不是金矿 |
| 5 | **07-28** | **成交后 4.5 分钟内同盘 ≤0.69 新挂单被他人吃走** | **约 85 股落在我方限价 0.69 之内**，全部结算 $1。跟随者共吃 116.25 股，价格只推到 0.70 | **+$26 毛利（唯一真金）** |

**第 5 条是唯一确实可做、确实错过、确实有钱的。** 根因是 [tradeExecutor.ts:858](lib/polymarket/tradeExecutor.ts:858) 的去重逻辑：`ledger.find(e => e.tokenId === input.tokenId && !e.probe && exposedUsd(e) > 0)` → 一旦有敞口该 token 永久 skip，不看剩余额度、不看当前价。而 $47 这个数字本身也不是额度闸限制的，是 [tradeExecutor.ts:995](lib/polymarket/tradeExecutor.ts:995) 按当刻限价内深度 `depthUsd×0.9` 定量的结果（深度约 $52）。

**被证伪的两条**（别去做）：
- ~~maker 挂单能吃残余点差~~ — 该盘信号后到结算的 taker **SELL 为 0**，我方挂 resting BID 无对手方；两条空盘腿 ±2h 内亦零成交。maker 路线目前是"未证实"，不是"已验证的机会"。返佣本身量级仅约 $0.4/单。
- ~~同事件多腿会被互斥闸误杀~~ — 飓风三腿经 Gamma 实测 `negRisk=false`，不是 neg-risk 家族，三腿同 tick 全部正常进入执行器。反向的真问题是：系统**没有任何同事件集中度约束**，同一 event 下多个市场可各自放行到 $100，唯一约束是日额 $300。

---

## 二、市场这几天变了什么

| 变化 | 实测证据 | 要不要改 |
|---|---|---|
| **费率表换了** | Gamma `feeSchedule` 实测（top-100 在架）：`rate` 0.04/0.05/0.07 三档 + **`rebateRate` 0.15/0.20/0.25 新字段**；**23/100 市场 `feesEnabled=false`**。sports 07-10 起 0.03→0.05 | **代码无需改**——`takerFeeOf` 读 `feeSchedule.rate`，公式 `rate×(1−p)` 与官方一致。只需更新文档/记忆里"8 品类 3–7%"的旧基线 |
| **data-api 放宽** | `/trades` 分页上限 **4000→10000**，新增 `start`/`end` 时间窗 | 回测侧改动。bt4 遗留的"巨盘掉出经济样本"盲区**可彻底消除** |
| **Polygon 出块 2s→1.50s** | 5 段独立测量，精确 1.5000 s/块 | **要改**：[onchainEvents.ts:47-48](lib/polymarket/onchainEvents.ts:47) 的 `129_600`/`43_200` 按 2s 写死 → 实际回补只有 2.25 天 / 18 小时。影响 scan-notify 的争议扫描（chain-watch 实弹链路有自己的 gap 告警，不受影响） |
| **模板盘洪水** | 07-31 单日 **1,311 事件**（其余 8 天日均 10–52），AI Arena + 足球赛程模板；打爆 LLM 预算，956 次判读被跳过 | **不当缺陷修**（被丢的属 bt3 期望为负的类别）。该做的是入队前按标题模板去重限流，保护 🟢 路径的预算 |
| **CLOB v1 NegRisk Adapter 弃用** | 747/747 在架 negRisk 盘仍走旧地址 | **不要动** `execCheck.ts:154` 的常量 |
| **Gamma offset 分页将弃用** | keyset 端点已可用 | 技术债，不紧急。真弃用会让扫描端全哑 |
| **竞争没有加剧** | 我们是澄清后第一笔成交，**领先第二名 34 秒**；残留 30 个点两小时无人吃 | 不要为抢速度投入 |

**一句话**：没有任何变化威胁到策略本身，唯一必改的是出块时间常量。费率变化反而是好消息（部分品类免 taker 费）。

---

## 三、现在的问题

### P0

**1. 今天 09:10 CST 的日志截断会销毁全部执行留痕**
crontab 那行是 `tail -c 500000` 丢弃式截断。实测：9 条 `chain-watch-trade` 明细行最大字节偏移 1,312,577，保留起点 1,866,482 → **9/9 全部落在被丢弃的头部**，4,225 条 tick 漏斗汇总行同样。其中 6 条 closed-skip 在 `trade-ledger.jsonl` 里**没有任何副本**（`e.closed` 分支在 `executeSignal` 之前 return），`notified` 500 条滚动窗口也已把 9 个 qid 全部淘汰。

**已处置**：三份日志全量副本已抢救到 `tmp/review-2026-08-02/logs-rescue/`（00:48 快照，生产未改动）。**机制未修，每周复发。**

### P1

**2. 零留档，8 月中 go/no-go 没有任何前瞻样本**
四处互为因果：
- `scan_runs / opportunities / odds_snapshots = 0` — [scan-notify.ts](scripts/scan-notify.ts) 从未调用 `persistScanResult()`
- `paper_trades = 0` — [chain-watch.ts:1455](scripts/chain-watch.ts:1455) 的 `!e.fill100` 要求模拟 $100 市价单**吃满**，而实测最厚一腿深度只有 ~$52。**连唯一那笔真实成交也没登记**（I6 的承诺从未兑现）。连带死锁：`EXEC_FORECAST_LIVE` 默认 off，解除条件是 paper 验证，而 paper 在当前深度下取不到样本
- `llm-stance-cache.json` 上限 300 条，07-31 洪水一次写满，之前全部判读历史被挤光
- `notified` 指纹表 500 条：466 rule_context / 15 none / 19 reset，**零条方向性、无时间戳、无 tier、无信号价**

*注：8/9 的缺样本来自"没有可交易盘口"，`fill100` 门槛只能独立解释 9 次里的 1 次——它是唯一可修的那一档，但不是主因。*

**3. 4 个 RPC 只剩 2 路活，而告警门槛永远够不着**
逐个实测：publicnode ✅ / **nodies 403 付费墙 ❌** / tenderly ✅ / **1rpc HTTP 200 但 body 是 `-32001 usage limit` ❌**。日志窗口内 8 次 `sweep made no progress` fatal（~6.4 次/周，占 tick 0.19%）。heartbeat 按 marker 文件 mtime + `staleMinutes=15` 判活，需连续 5 tick 全挂才告警，散点失败永远触不到。
直接期望损失 <$1/月；**尾部风险是两路同挂 = 信号通道全盲且不告警**。实测唯一可用的替换候选是 `polygon.drpc.org`（ankr / llamarpc 现均要 API key）。

**4. scan-notify 通知闸门无方向性**
[scan-notify.ts:77-78](scripts/scan-notify.ts:77) 规则 (a)：`uma==="disputed" && decision==="actionable" && netReturnPct>=0.03` —— **不看 stance、不看方向、只有 3% 地板没有价格天花板**。它是 `stance=null` / `price_fallback` / `dispute_notice` 三档共 **21/28** 条信号的唯一入口。后果：近 4 周 21 个市场里 **7 个两条腿都收到了通知**；发出过 net80.8%（≈0.55 掷硬币腿）这种通知，而这个 cohort **回测样本量 n=0**。
13 天 39 封邮件里 **18 封是 🟠 官方背书 leans_*·medium**（17 封是 leans_NO），**零封达到 chain-watch 的 🟢 口径**。一个市场（"Iran agrees to end enrichment"）8.6 天发了 **11 封逐字相同的邮件**——`isNewOrChanged` 只折叠了 `rejected↔observe`，`actionable↔rejected` 的抖动仍触发重发。
*当前未接执行器，直接损失 $0。真实成本是注意力污染：13 天里唯一值钱的那封（07-28 飓风）被埋在 38 封噪音里。*

**5. chain-watch.log 全文零时间戳** — 9,163 行 grep ISO 时间戳零命中；`logTradeSkip` 只输出 `{mode, qid前12字符, token前12字符, status, reason}`，无 `at`、无 question、无 bestAsk。本次复盘为还原 5 条记录被迫做链上考古，而**五家公共 RPC 对 6 天前的 `getLogs` 全部拒绝**，只有 tenderly 可用——这条取证路径随时会断。

### P2

**6. `totalLeft` 用毛敞口计算** — [tradeExecutor.ts:912](lib/polymarket/tradeExecutor.ts:912) 只有 `openTotal >= totalMaxUsd` 时才做结算净销，未触顶时用没净销过的毛值。→ 历史累计成交 $795（全部早已结算）时 `totalLeft=$5`，订单被静默缩到 $0，而净敞口其实是 0。当前毛额 $47，按现节奏约一年后触及。

**7. `llmPending` 硬顶 50 静默淘汰** — [chain-watch.ts:1271](scripts/chain-watch.ts:1271) 队列超 50 直接 `delete`，**无日志、不进 digest**。07-31 洪水按队列复算淘汰 404–413 条，走有痕路径的只有 8 条（2%）。与 M4 修掉的 null 吞单是同一反模式。*本次实测损失 ≈$0（被丢的属期望为负类别），但 07-26/07-28 那种姊妹盘簇若落在洪水窗口会被同样淘汰。*

**8. 交易私钥全世界只有一份** — 在那台 07-13 曾停摆 6.3 小时的 WSL 上，无任何备份。暴露 $823.15 + 重建 CLOB 凭据与 owner 白名单的时间成本。

**9. heartbeat 探不到"跑着但一笔都不做"** — 5 个告警面全是"进程/依赖存活"，业务活性（`exec_checked` / `llm_cli_calls` / `trade_filled` / `paper_registered`）在 `chainStats` 里根本不被解析。RPC 散点 fatal、连续零 🟢、pUSD 余额异动、`EXEC_FORECAST_LIVE` 长期忘记开——对告警全透明。

### 顺带澄清的两条

- **链上赎回不用做了**：07-28 那笔在成交后 1h59m 由第三方批量赎回交易自动到账（tx `0x4131fa16…`，同笔代赎 7 个地址），PredEdge 未做任何动作。*但 n=1，代赎方身份未证实为官方，不能把风险直接划掉。*
- **抵押品已从 USDC.e 迁到 pUSD** `0xc011a7e12A19F7b1F670D46f03b03f3342e82dfb`，proxy 现存 **823.15 pUSD**，USDC.e 与原生 USDC 均为 0。**任何按 USDC.e 查余额的监控都会读到 0 并误判。**
- **生产与 git main 零漂移已实测确认**（70 文件全量 md5），46/46 测试全绿但只覆盖纯函数闸门与记账。

---

## 四、动作清单（按 ROI 排序）

### 值得做

| 优先 | 动作 | 依据 | 预期 | 成本 |
|---|---|---|---|---|
| **1** | **成交后复访补仓**：把 tokenId 去重从"二值封锁"改成 per-token 累计敞口上限（先设 = `maxOrderUsd` $100）；对已成交未结算的 token 挂轻量复访队列，每 3 分钟重拉 book，价仍 ≤ 原 limitPrice 就追加 FAK | 07-28 实测 85 股落在自家限价内被他人吃走 | **+$20~35/笔**，唯一有实证的增量 | 中（新增复访队列） |
| **2** | **日志轮转**（`mv` + gzip 保留 8–12 周，替掉 `tail -c` 丢弃）+ **执行留痕独立落盘**（`logTradeSkip` 同时 append 到 `data/trade-attempts.jsonl`，不受轮转影响）+ **日志加时间戳** | 9/9 留痕落在被丢弃区；6 条 closed-skip 无其他副本 | 使能项，决定 go/no-go | 低（各 3 行） |
| **3** | **机会落库**：scan-notify 在 `resolveOpenPaperTrades()` 前插 `persistScanResult`，try/catch + 惰性 require 保持"无 sqlite 也能跑" | 四表全 0 行 | 使能项，攒 3 周才有资格谈接执行器 | 低（4 行，实测 21ms/+303KB） |
| **4** | **换掉两个已死 RPC**（→ `polygon.drpc.org`，或上付费）+ heartbeat 加第 5 探针（逐个打 `eth_blockNumber`，存活 <2 即告警） | 4 路只剩 2 路，散点失败触不到告警 | 尾部风险，单次肥尾四位数 | 低 |
| **5** | **三处小修**：`totalLeft` 净销提前到 50%；`onchainEvents` lookback 常量 →`172_800`/`57_600`；`paper_trades` 登记门槛从 `!e.fill100` 改为"记录 + 标注原因"（即使深度不足也写一行，带 `skipReason`/`bookEmpty`） | 静态确定 | 各自 $10~15/笔量级 + 解锁 paper 样本 | 低 |
| **6** | **scan-notify 收窄（止损，不增收）**：规则 (a) 加方向性与条件、对 `DIRECTIONLESS_STANCES` 一律不发 actionable、加 12h 同 token 冷却 | 39 封里 18 封是历史亏损档，11 封是同一市场重复 | 减噪，不是增收 | 低 |
| **7** | **私钥离线备份** — **这一步必须你本人手工做**，私钥不能经由会话读取或搬运 | 单点 | 保 $823 | 你的 10 分钟 |
| **8** | **同事件集中度闸**：按事件累计同 tick 敞口，上限 $150 | 现在唯一约束是日额 $300 | 风控补洞 | 低 |

### 明确不要做

- ❌ **放松任何价格/额度/滑点闸门** —— 8 类风控闸生产触发 **= 0 次**，放松它们的收益上限实测 **$0**。
- ❌ **调高 `EXEC_MAX_ORDER_USD`** —— 5 分钟内可吸收 taker 量仅约 $130，加钱只会吃穿盘口。
- ❌ **为抢速度投入** —— 已领先第二名 34 秒。
- ❌ **上 maker 挂单** —— 该盘 taker SELL 为 0，无对手方，目前"未证实"。若要试，先离线统计 🟢 事件后 N 分钟内的 taker SELL 到达率，别直接写代码。
- ❌ **把 scan-notify 的 actionable 接进执行器** —— 期望 ≈0 到负，且那条链路**零 LLM 判读、零 tier**，接线等于缺一整套判读层。
- ❌ **修 07-31 洪水的"漏判"** —— 被丢的 956 条属 bt3 期望为负类别，修了可能是少赚变多亏。

### go/no-go 的替代口径（8 月中）

paper 池取不到样本、实盘 n=1，统计判定这条路今年走不通。三条替代按可信度：
1. **邮件归档回放** —— 每个 🟢 都发过邮件，MAIL_TO 邮箱是唯一没被截断的 append-only 时间序列，能同时给出 tier / 价格 / 时间。把 07-05 上线以来的邮件结构化，用 data-api 真实成交 + Gamma(`closed=true`) 回填结算。
2. **落库后从 8 月重新计时**，8 月中只做"工程就绪度 + 存量先验"二元判定，统计判定推迟。
3. 用 data-api 新的 `start/end` + `limit=10000` 重建离线回测通路（顺带复核 bt4 巨盘盲区）。

---

## 五、诚实清单

- **对抗核验的合并环节我第一次写错了**（按标题精确匹配，核验方加了编号前缀），导致 47 条全部落成 `UNVERIFIED`。已按"配对 + 索引"重新对齐，逐条抽查语义一致。裁决分布 20 CONFIRMED / 25 PARTIAL / 2 REFUTED。
- **我在初步快照里说 07-28 两腿"方向全对被白丢了"是错的**——data-api 实测两盘 07-27 之后到结算零成交，机会成本 ≈$0。
- **样本量普遍很小**：实盘 n=1，🟢 可执行腿 n=1/8.8 天，scan-notify 已结算 n=14。任何按这些数字年化的结论都脆弱。
- **未证实**：maker 路线的对手方到达率；代赎方是否为官方；跨市场的澄清后收敛速度（只测了 1 个市场）；通知深度分布（零留档）。
- **可观测窗口只有 8.8 天**（日志周截断），无法判断官方澄清频率的趋势——只能说"采集端无缺口"，不能说"信号源已证明未衰减"。1,512 事件里 1,311 来自 07-31 单日洪水，扣除后仅 ~25 事件/天。
- **物理约束，无解**：空盘时 taker 任何价位不可成交（CLOB `/book` 全镜像）；结算后补发澄清是官方主流行为（6/9），事件驱动路径的可交易密度**结构性低于** bt3 回测口径，回测信号数不能直推实盘。
- 生产全程只读，未下单、未改配置、未动 crontab。
